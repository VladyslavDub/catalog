<h1>
  Wellcom to <%= link_to "Catalog", products_path%>
</h1>
<hr>
<div class="">
  <h2>some product from Category <%=link_to @category.name, category_path(@category)%></h2>
  <div class="row">
    <%=render ProductComponent.with_collection(@category.products.limit(4))%>
  </div>
</div>
<hr>
<div class="row">
  <%=render ProductComponent.with_collection(@products)%>
</div>